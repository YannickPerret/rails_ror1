<%= form_with(model: class_subject_semester) do |form| %>
  <% if class_subject_semester.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(class_subject_semester.errors.count, "error") %> prohibited this class_subject_semester from being saved:</h2>

      <ul>
        <% class_subject_semester.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :school_class_id, style: "display: block" %>
    <%= form.text_field :school_class_id %>
  </div>

  <div>
    <%= form.label :subject_id, style: "display: block" %>
    <%= form.text_field :subject_id %>
  </div>

  <div>
    <%= form.label :semester_id, style: "display: block" %>
    <%= form.text_field :semester_id %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
