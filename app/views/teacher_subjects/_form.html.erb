<%= form_with(model: teacher_subject) do |form| %>
  <% if teacher_subject.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(teacher_subject.errors.count, "error") %> prohibited this teacher_subject from being saved:</h2>

      <ul>
        <% teacher_subject.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :teacher_id, style: "display: block" %>
    <%= form.text_field :teacher_id %>
  </div>

  <div>
    <%= form.label :subject_id, style: "display: block" %>
    <%= form.text_field :subject_id %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
