<%= form_with(model: teacher_teach_branch) do |form| %>
  <% if teacher_teach_branch.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(teacher_teach_branch.errors.count, "error") %> prohibited this teacher_teach_branch from being saved:</h2>

      <ul>
        <% teacher_teach_branch.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :branch_id, style: "display: block" %>
    <%= form.text_field :branch_id %>
  </div>

  <div>
    <%= form.label :teacher_id, style: "display: block" %>
    <%= form.text_field :teacher_id %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
