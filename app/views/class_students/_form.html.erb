<%= form_with(model: class_student) do |form| %>
  <% if class_student.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(class_student.errors.count, "error") %> prohibited this class_student from being saved:</h2>

      <ul>
        <% class_student.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :student_id, style: "display: block" %>
    <%= form.text_field :student_id %>
  </div>

  <div>
    <%= form.label :school_class_id, style: "display: block" %>
    <%= form.text_field :school_class_id %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
